#include "main.h"

/*
 * print_rev(char *s) - print string in reverse
 *
 * return: always 0
*/

void print_rev(char *s)
{
	int i = 0;
	int r;

	while (s[i] != '\0')
	{
		_putchar(s[i]);
		i++;
	}
		_putchar(r[i])
			s--;
		_putchar('\n');
}
