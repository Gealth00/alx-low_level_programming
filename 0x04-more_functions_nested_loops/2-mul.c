#include "main.h"
#include <stdio.h>

/**
 * mul - multioles two integers
 * @a: number being tested
 * @b: number being tested
 * Return: Always 0.
*/

int mul(int a, int b)
{
	int product;

	product = a * b;
	return (product);
}
